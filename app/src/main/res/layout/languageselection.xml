<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Top Bar (Language title) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingBottom="16dp"
            tools:ignore="ExtraText,UseCompoundDrawables">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_back"
                tools:ignore="ContentDescription" /> <!-- Use your back arrow icon -->
            android:contentDescription="Back" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Language"
                android:textSize="18sp"
                android:textStyle="bold"
                android:gravity="center"
                tools:ignore="HardcodedText" />
        </LinearLayout>

        <!-- Current Language Section -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="4dp"
            android:text="Current Language"
            android:textColor="#888888"
            android:textSize="12sp"
            tools:ignore="HardcodedText,TextContrastCheck" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="English"
            android:textSize="16sp"
            android:textStyle="bold"
            android:paddingBottom="16dp"
            tools:ignore="HardcodedText" />

        <!-- Language Options -->
        <RadioGroup
            android:id="@+id/languageRadioGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <RadioButton android:text="English" android:id="@+id/langEnglish"
                tools:ignore="HardcodedText" />
            <RadioButton android:text="हिंदी (Hindi)" android:id="@+id/langHindi"
                tools:ignore="HardcodedText" />
            <RadioButton android:text="தமிழ் (Tamil)" android:id="@+id/langTamil"
                tools:ignore="HardcodedText" />
            <RadioButton android:text="ಕನ್ನಡ (Kannada)" android:id="@+id/langKannada"
                tools:ignore="HardcodedText" />
            <RadioButton android:text="తెలుగు (Telugu)" android:id="@+id/langTelugu"
                tools:ignore="HardcodedText" />
            <RadioButton android:text="മലയാളം (Malayalam)" android:id="@+id/langMalayalam"
                tools:ignore="HardcodedText" />
            <RadioButton android:text="বাংলা (Bengali)" android:id="@+id/langBengali"
                tools:ignore="HardcodedText" />
            <RadioButton android:text="ગુજરાતી (Gujarati)" android:id="@+id/langGujarati"
                tools:ignore="HardcodedText" />
        </RadioGroup>

        <!-- Apply Button -->
        <Button
            android:id="@+id/apply_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:backgroundTint="#007BFF"
            android:padding="12dp"
            android:text="Apply Changes"
            android:textColor="@android:color/white"
            tools:ignore="HardcodedText,TextContrastCheck,VisualLintButtonSize" />
    </LinearLayout>
</ScrollView>
